# Should be loaded into root
- !policy
  id: secrets
  body:
  - &variables
    - !variable db_username
    - !variable db_password

- !permit
  resources: *variables
  role: !host conjur/authn-k8s/{{ CONJUR_ACCOUNT }}/apps/{{ PROJECT_NAME }}/*/*
  privileges: [ read, execute ]